{
  "name": "server",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts --name monolog-server",
    "lint": "biome lint ./src",
    "format": "biome format ./src",
    "pre-push": "pnpm run lint && pnpm run format && pnpm run test",
    "test": "jest"
  },
  "dependencies": {
    "hono": "^4.5.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "@cloudflare/workers-types": "^4.20240529.0",
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.4",
    "wrangler": "^3.57.2"
  }
}
